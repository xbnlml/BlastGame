# BlastGame 依赖
# 分两组安装：
#   工具链（纯 Python，无 Unity 也能跑）: pip install openpyxl
#   RAG（需 python 3.11，numpy/faiss 为 cp311 构建）: pip install -r requirements.txt

# --- 工具链（全部只读/入库工具依赖）---
openpyxl>=3.1

# --- RAG 检索学习项目（python 3.11 专用）---
# 注意：sentence-transformers/faiss-cpu 只有 cp311 wheel 可用，
# 用 python3.11（或 hermes venv）安装运行，见 rag/README
sentence-transformers==5.6.0
faiss-cpu==1.15.0
numpy==2.4.3