# blastgame SKILL.md 瘦身方案（163KB → ~50KB）— 2026-08-14

> 现状实测：`SKILL.md` = **166,039B（162KB）**，已超 skill_manage 100KB 硬限。
> 本方案全部数字为**实测**（`scripts/slim_blastgame_skill.py` dry-run 已跑通，产物见 `hermes/_slim_draft/`）。

---

## 0. 分层原则（先立规矩，再谈切割）

知识分三层，按"加载成本 × 使用频率"决定放哪：

| 层 | 加载方式 | 适合内容 |
|---|---|---|
| **L1 SKILL.md 常驻** | 每次触发 skill 全量进上下文（~45KB 预算） | 硬性铁则、入口路由、**现行有效**的判定/流程标准、症状→指针索引 |
| **L2 references/ 按需** | skill_view(file) 触发才读（640KB 预算，不占常驻） | 完整坑表、详细流程、历史决策、审计报告 |
| **L3 RAG 检索** | 向量+BM25 混合查（语料=BlastGame Doc） | 开放查询型领域事实（游戏设计文档） |

**当前病根**：SKILL.md 把 L2/L3 的内容（67KB 坑表、15KB 索引、历史日志）全塞进了 L1，每次加载白付 100KB+。
**修复铁则**：SKILL.md 只留"不读就犯错"的内容；"读才需要"的全部外移。

---

## ① 常见坑速查（68.5KB，132 条）→ 挪去哪

**答案：新建单一 reference `references/pitfalls.md`（完整条目 + 域锚点），主文件只留 ~6KB 症状索引。不进 RAG。**

理由（三选一的决策逻辑）：
1. **不进 RAG**：坑表是"确定检索"（已知症状 → 查已知修复），不是"开放检索"（不知道该怎么问）。RAG 混合检索 recall@1≈80%，对**排障必须命中**的场景不够可靠——查不到坑 = 重走老路。且当前 RAG 语料是 BlastGame Doc（游戏设计文档），混入排障知识会污染语义空间、需要重建索引+扩 golden QA，成本高收益低。
2. **不拆成 90 个文件**：INDEX.md 自己已承认"84 个 reference 导致检索失效（有记录但没被触发）"。拆越多越难触发。**一个坑表文件 + 主文件症状索引**是最短触发链。
3. **代价可控**：`skill_view(pitfalls.md)` 全文 69KB ≈ 1.7 万 token，只在"遇坑时"付一次；而塞在 SKILL.md 里是**每个 session 每次触发都付 67KB**。

**主文件里的症状索引（替代原坑表，~6KB）**：格式见 §③。

---

## ② 主文件保留什么（L1 内容清单）

**保留（不读就犯错 / 每次必用）**：
- 顶部环境事实 + 入口路由（L13-30，压缩 2026-08-11/12 日志为 1 行）
- 「什么时候用」触发条件（L23）
- **动手前必查现有工具**（L31，用户暴怒铁则，保留全文）
- Agent 架构与分工（L43，精简到 1.2KB）
- 全自动流水线主流程（L62，精简到 1.5KB）
- **判定标准全部现行子节**（L99-224，~8KB）——每次判定必用，但**删除历史演进段**：纯权重 5.0 探索过程（L174 内）、"旧版 ≤2pp 豁免已删除"等被替代描述只留一句
- 改关卡判定四信号（L225-254，精简）
- 探针设计理念核心（L321-333，精简到 1.2KB，细节指向 probe-efficiency-standards）
- Warden 闸门（L342-374，精简）
- 破坏性操作永禁（L415-421，保留全文）
- 命令速查（L682-714，保留）
- 坑速查症状索引（新，~6KB）
- References 精简速查（新，~2.5KB，指向 INDEX.md）

**外移/删除（移到 L2，只留摘要 stub ~1.1KB/段）**：

| 段 | 现状 | 处置 |
|---|---|---|
| 数据可靠性（pool）L256-270 | 6.8KB | → `references/pool-data-reliability-20260814.md`，主文件留 4 行铁则摘要 |
| 中控关卡数据库 L271-313 | 8.5KB | → `references/leveldb-essentials-20260814.md`，主文件留"DB 是另一权威视图 + fingerprint 匹配 + 验证脚本"摘要 |
| 用户沟通铁则 L389-414 | 9.1KB | → `references/user-communication-rules-20260814.md`，主文件留 5 行最高频铁则（等确认/不擅自跑批/展示完整信息） |
| DDA 运行时参数 L422-440 | 0.9KB | → 指向现有 `references/dda-runtime.md`，删 |
| 工具链修复精华 L441-450 | 1.4KB | 删（已有 `references/tool-fixes.md` 完整 33 条） |
| 常见坑速查 L451-589 | 67KB | → `references/pitfalls.md` + 症状索引 |
| 入库落盘 L590-628 | 9.3KB | → `references/reimport-write-down-20260814.md`，主文件留"落盘三/四动作 + 防白关验证"摘要 |
| 只读现状查询 L629-644 | 3.5KB | → 指向 `scripts/level_status.py`，留 0.5KB |
| References 索引 L715-774 | 15.5KB | → 2.5KB 精简速查（10 个核心文件表）+ 指向 `references/INDEX.md` |

---

## ③ 坑表结构（触发式索引）

**主文件（L1）症状索引** —— 只留"关键词 → 锚点"两列表（~6KB，13 行）：

```markdown
## 坑速查（症状 → 查 references/pitfalls.md 锚点）
| 症状关键词 | 查锚点 |
|-----------|--------|
| 判定分档 / flat 10pp / Hard SuperHard 合格线 / gap 接近线 | #judgment |
| phase1 phase2 / filter_verified / 数据源优先级 / 同配置胜率差大 / dedup 埋数据 | #data-source |
| 探针设计 / 反推法 / sd 方向 / 探针打空 / 组合 6 轮不变 | #probe |
| auto_loop 超时 / planner FAILED / sys.path / ModuleNotFoundError / 轮次 | #auto-loop |
| DB 白关 / fingerprint / resolveActiveRun / 单档 entry | #leveldb |
| Excel 单位 / 小数百分数 / 就地更新 / board 行更新 | #write-down |
| 贝叶斯提前停 / adaptive-stop / 局数标准 | #auto-loop |
| 用户暴怒过的行为 / 沟通铁则 | #communication |
| ✅已修历史坑 | #archive |
```

**pitfalls.md（L2）内部结构** —— 9 个域锚点，132 条**保留原编号**（编号是主文件/日志/代码注释的引用键，绝不重排）：

```markdown
# blastgame 常见坑速查（完整表）
## #judgment   判定分档/容差/目标偏差      （坑 1,12,13,31,31b,36,103,109,117...）
## #data-source 数据源/池子/时间防线/牌面校验 （坑 2,40,41,48,50,53,89,96,102,104,105,106,108...）
## #probe      探针设计                     （坑 3,4,20,21,24,33,34,49,56,57,113...）
## #auto-loop  流水线/时序/超时/进程         （坑 18,19,42,72,73,76,83,85,111,112,129...）
## #leveldb    关卡数据库                   （坑 85,90,91,93,96,97,122,123,125,129...）
## #write-down 入库落盘/Excel/board          （坑 47,51,52,98,99,100,101,107,118,119,124,126...）
## #toolchain  工具链/脚本/路径/编码          （坑 25,27,37,46,77,78,79,80,87,94...）
## #communication 用户沟通/流程铁则           （坑 23,35,54,88,95...）
## #archive    已修历史坑（✅/已修复）        （坑 14,15,16,17,110,124,126,127...）
```

**触发协议（写进主文件）**：遇症状 → 查索引定位锚点 → 读该锚点坑条目；索引没命中 → 直接 `skill_view(pitfalls.md)` 读全文一次。**新增坑一律追加进 pitfalls.md 对应锚点**，不许再写回 SKILL.md（防复发关键）。

---

## ④ 割让预算表（163KB → ~50KB，已实测验证）

| 步骤 | 字节变化 | 结果 |
|---|---|---|
| 现状 | — | 166,039B |
| 切掉 7 个重段（坑表/沟通铁则/入库/数据可靠性/LevelDB/探针细节/只读查询） | −108KB | 57,987B |
| References 索引 15.5KB → 2.5KB | −13KB | ~44,937B |
| 顶部 08-11/12 日志压缩 | −1KB | ~43.9KB |
| **加回**：坑速查症状索引 6KB + 6 个摘要 stub（~6.6KB） | +12.6KB | **~52.5KB（=51.3KB）** |
| 判定标准段删历史演进/被替代方案 | −4KB | **~48.5KB** |

**实测 dry-run 结果**（`scripts/slim_blastgame_skill.py`，已跑）：
- 坑表外移文件：`_slim_draft/pitfalls.md` = 69.3KB（含分组头）
- 新主文件：`_slim_draft/SKILL.md` = **43.3KB**（不含 6 个摘要 stub，人工补摘要后 ~50KB）
- ✅ 远低于 100KB 硬限，达成 50KB 目标

**执行方式**（脚本已就绪）：
```bash
cd D:/download/BlastGame/hermes
python scripts/slim_blastgame_skill.py          # dry-run：产物在 _slim_draft/
# 人工补 6 个 stub 摘要 + pitfalls.md 分域锚点（机械切割已完成）
python scripts/slim_blastgame_skill.py --apply  # 备份 SKILL.md.bak-<ts> 后落盘
```
> ⚠️ 落盘前先跑一次 skill 触发冒烟测试（加载新 SKILL.md + 抽查索引能否定位坑 103/105）。

---

## ⑤ MEMORY.md 清理规则（34.3KB / 74 条 → 目标 ~15KB / ≤32 条）

**总规则**：MEMORY 只留"**下次会话必须知道的、且 skill 里查不到或太重**"的信息。凡已 100% 落进 SKILL.md/references 的，MEMORY 删或压成一行指针。

### 5.1 四条删除规则（可复制执行）

**R1 已修 bug 记录 → 删**：修复已进代码且坑已入 pitfalls.md，则事故细节删。判据：`修复：` 后的方案能在代码/文档找到，且不是"会复发"的陷阱。
- 例：#43 的 `rnd=inc_round` 修复细节（skill 已记）→ 删；#62 payload 格式细节（skill #125 + references/toolchain-review 已有）→ 压成一行。
- **例外保留**：会复发的陷阱（Excel 小数 vs 百分数单位、`read_ddc` 返回字符串、`sys.path` 缺失）→ 属于规则类，保留。

**R2 单日事故/一次性审计结果 → 删**：纯数字型结论（"X 条匹配 Y 条不一致 Z%"）且 references 已有对应文件 → 删。MEMORY 不存"测量快照"，只存"影响未来行为的规则"。
- 删：#56（bot400 342 条 65% 不一致——`references/bot400-vs-db-consistency-20260810.md` 完整版已在）、#60（78 条 43 绿 55%——同文件）。
- 保留其**规则化结论**一行："summary 入库记录会被 bot400 证伪，新入库必须 bot400 复核"（#56/#60 的教训，skill 也已记，若 skill 有则连这行也删）。

**R3 被替代方案 → 删**：新规则已定稿且记录在案，旧规则/旧格式/旧参数一律删，至多留"从 X 改为 Y"半句防混淆。
- 例：target_deviation.max 15→10（留新的，删旧的）、纯权重 5.0（被分段评分替代，skill L174 已标）、旧 board 三列表格式（#27 已记新格式，删旧描述）、verified 200 局→10 局语义（#76 留现行）。

**R4 面试/RAG 学习域 → 移出（不是删）**：非 BlastGame 流水线内容，挪到 `agent-career-learning` skill 的记忆或独立文档，别占全局 MEMORY。
- 移出：#58 AI 岗位面试准备、#59 学习方式偏好（可在 agent-career-learning 留 1 行精简版）、#63 课程文件位置、#64 RAG 项目建成跑通、#66 RAG python3.11 运行细节。
- **保留**：#65 主动找答案、#67 面试素材与生产代码分离——这两条是**通用工作偏好**，适用一切项目（#67 原文自己写了"此偏好适用于一切项目"）。

### 5.2 压缩规则

- **单条 ≤300 字符**（现在最长 615）。压法：留"规则本身 + 触发条件"，删案例细节/日期/引用链。
- **综合条目拆条**：#36/#40/#43/#45/#48/#53/#54/#68 等"【日期】①…②…③…"大杂烩 → 拆成单规则，与 skill 重复的删，保留的去重。
- **skill 有常驻版的规则**（#5/#9/#13/#14/#15/#20/#21/#22/#24/#26/#27/#28/#30/#31/#46 等在 SKILL.md 判定/流水线/铁则段有对应）→ MEMORY 只留 1 行："见 blastgame skill §判定标准 / §坑速查 #xx"，防止双源漂移。

### 5.3 目标与节奏

- 74 条 → **≤32 条**；34.3KB → **≤15KB**（回到 20KB 预算内）。
- 配额：铁则类 12 条 + 数据语义类 10 条 + 用户偏好类 6 条 + hermes 配置类 2 条 + 环境事实 2 条。
- **节奏**：每周日或每次"用户大改流程"后做一次；规则 = "新条落 MEMORY 前，先问：skill 里有没有？没有才进，且压到 300 字符"。

### 5.4 74 条逐条处置表（速查，完整规则见上）

- **移出（interview/RAG，8 条）**：#58 #59 #63 #64 #66（+ #67 保留精简）
- **删（一次性审计，2 条）**：#56 #60
- **删/压（已修 bug 且 skill 已记，~8 条内）**：#40 的 rnd 部分、#43 的 watchdog 已修部分、#62 的 payload 细节、#2 的 93 关误报细节等
- **拆+压（综合大条目，8 条）**：#36 #40 #43 #45 #48 #53 #54 #68
- **压成 1 行指针（skill 常驻重复，~15 条）**：#5 #9 #13 #14 #15 #20 #21 #22 #24 #26 #27 #28 #30 #31 #46
- **保留（铁则/现行标准/数据语义/偏好，~32 条）**：其余全部

USER.md（25.9KB/92 条）同规则：interview 偏好移出、重复合并、单条压缩。

---

## ⑥ 哪些内容该进 RAG

**本次瘦身不依赖 RAG**（坑表/流程都走 L1/L2，可靠性高于 RAG）。RAG 只作为**可选第二阶段**：

| 内容 | 是否进 RAG | 理由 |
|---|---|---|
| 坑表 / 判定标准 / 数据可靠性规则 | ❌ 不进 | 触发式排障，recall 不够；判定标准频繁变（rules.json 单一真源），RAG 必陈旧 |
| 稳定领域事实：DDA 公式、DB 结构、关卡分段、探针反推法 | ✅ 可进（独立索引） | 开放查询型知识；但须**新建 skill-refs 独立索引**（`RAG_CORPUS_ROOT` 指向 references 子集），勿混入 Doc 语料；golden QA 扩展后再用 |
| 游戏设计文档（现状语料） | ✅ 已是 | 保持现状 |

> 优先级：L1/L2 瘦身（本周）> MEMORY 清理（本周）> RAG 独立索引（可选，下周）。

---

## ⑦ 防复发机制（写完即生效）

1. **新坑只进 `references/pitfalls.md`**，主文件只加一行症状索引——写回 SKILL.md 视为违规。
2. **新 reference 必登记 INDEX.md**（已有约定）；索引 90+ 文件上限后按 INDEX 分组不再增文件，优先合并进主题文件。
3. **skill_manage 写入前自查**：SKILL.md 目标 ≤55KB；超了先外移再写（可在 hook 加 size 检查）。
4. **MEMORY 写入前自查**：一条一问"skill 里有吗？"；单条 ≤300 字符。
5. 每周清理日：坑表归档到 #archive、MEMORY 按 R1-R4 扫一遍。

---

## 产物清单

- `hermes/scripts/slim_blastgame_skill.py` — 迁移脚本（dry-run 安全，`--apply` 落盘，先备份）
- `hermes/_slim_draft/SKILL.md` — 新主文件草稿 **43.3KB**（待补 6 个 stub 摘要 → ~50KB）
- `hermes/_slim_draft/pitfalls.md` — 坑表外移草稿 **69.3KB**（待分 9 个域锚点）
- 本文件 — 方案说明
