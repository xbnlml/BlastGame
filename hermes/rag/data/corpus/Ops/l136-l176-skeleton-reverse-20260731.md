# L136/L176 探针目标 WR 整体反推（组合骨架视角，2026-07-31）

任务：不是逐档看缺口，而是先枚举"入库形态"的可行五档骨架（T1=T2、T4=T5 同配置 → 只需 **3 个不同配置**），再反推每档探针目标 WR。骨架约束 = 单调递减 + T1-T3∈[15,40] + T3-T5∈[15,40] + T3≥60 + |WR-目标|≤10（rules.json target_deviation.max）。脚本：`scripts/skeleton_enum.py`。

## 方法（可复用，3 模式枚举 + 逐槽定位）

1. **A 纯池子枚举**：三槽（T1=T2 / T3 / T4=T5）都取池子 verified 配置（去重键不含 tier，剔除 wr=0），枚举满足全约束的组合 → 两关都是 **0 可行** → 定位卡死槽位
2. **诊断**：先枚举 T1=T2+T3 两槽可行对 → 反推 T4=T5 需要窗口 = `[max(40, min(T3)-15), max(T3)-15]` ∩ [40,60] → 查池子该段条数（两关都是 0 条）
3. **B 目标骨架**：T1=T2/T3 取池子 + T4=T5 虚拟探针值半步枚举 → 最优目标骨架（探针产出后的最终形态）
4. **C 真实 judge 验证**：最优骨架（含虚拟槽）+ 豁免边界点直接喂 `judge_level.check_judgment(combo, 'normal', [85,85,65,50,50])`

## judge 输出读法坑（本次差点误判）

- `check_judgment` 对 gap 差 **≤2pp 有豁免**：gap=13-14.9 时 reasons 照列 `'T3→T5 gap=14.8<15pp'`，但 result 仍是 **合格**。**以 result 列为权威，reasons 出现 ≠ 不合格**（L136 T45=52、L176 T45=46 都被豁免放行）。探针"可用带"必须按豁免后边界算。
- **targets 是 list**（`[85,85,65,50,50]`），传 dict 会 `KeyError: 0`（`dev = abs(wrs[i] - targets[i])` 按整数下标）。

## L136 结论

- 池子 15 条 verified（66.75-97.0）；T1=T2 目标窗[75,95] 有 8 条，T3 窗[60,75] 有 6 条 → **都已覆盖**；40-60 段 0 条 = 唯一硬缺口
- **S1（最优）**：T1=T2=85.2（1,10,10,10,1 sd20）+ T3=66.8（uniform sd7）+ **T4=T5=50（探针）** → 偏差 +0.2/+1.8/0.0，gap 18.5/16.8，✅ 合格（全部目标 2pp 内）
- **T4=T5 探针目标 47-50%（最优 50）；可用带 [40, 53.7]**：52 豁免合格、55 不合格（gap 11.8）。T3 备选 68.75 时 T45 可放宽到 ≤53
- 备选骨架：T1=T2=89.25（uniform sd6）+ T3=73.75（10,10,1,1,1 sd17）→ gap13=15.5 太薄，弃
- 注意：T1=97（5,1,3 sd31）目标偏差 +12 → judge 降级"接近"，不能用于 T1=T2

## L176 结论

- 池子 14 条 verified（60.0-84.25）；T1=T2 窗[75,95] 有 8 条 ✅；**T3 窗只有 60.0（2,2,4,0 sd30 sc4，bot）一条可用**——72+ 全部因 gap13<15 被排除（T1=84.25 时 T3≤69.25）；61.32（middle-burst sd40）是 phase0，需 bot 400 复验才能入库
- **S1（最优）**：T1=T2=84.2（3,1,0,0,3,0 sd19 of0.01）+ T3=60.0 + **T4=T5=45（探针）** → 偏差 -0.8/-5/-5，gap 24.2/15.0，✅ 合格（gap35 零余量）
- **T4=T5 探针目标 43-45%（最优 45）；可用带 [40, 47]**：46-47 豁免合格、48 不合格（gap 12.0）
- **升级骨架（双探针）**：T3 探到 62-66 + T4=T5 探到 47-51 → 84.2/65/50，偏差 -0.8/0/0 ✅
- 对照：context 里的"现有最优 84.2/72.2/60.0"实测 **不合格**（gap 12.0/12.2 双 FAIL）——`find_best_monotonic` 结果必须过真实 judge 才能入库

## 可行性判定

- **L136 能否到 50 段：不能断定关卡太易，但路径全未验证**。支持有路：uniform sd7→66.8 断崖（sd6→89.2 跌 22.5pp）、phase1 middle-burst sd20→17% 弱提示（可达性证据）。反对：sd 非单调（sd24→68.8 后 sd27→84.8 反弹）、高 sd=容易区（sd31→97）→ sd30-50 通用模板必白跑。**判定标准：5 槽全打 40-55 段；若全 ≥55% → 关卡过易，改关卡**（level_sig 变更自动弃旧数据）
- **L176 探到 42-45 概率更高**：60.0（bot）+ 61.32（phase0）双证据，高 sd 走低方向成立

## 探针槽位建议（供 design_probes 参考，WR 目标为主）

- L136：5 槽全打 40-55 段 — uniform 低 sd 8-14 × of 0.7/0.9 延伸 sd7 断崖、middle-burst 1,1,10,1,1 sd8-16、10 重族 10,10,1,1,1 sd30 下探；sd 跨度 8→30 过 W01，3 种 ratios 过 W03
- L176：3 槽打 40-50（middle-burst sd45/50 of0.7、死列族 2,2,6,0 / 1,1,8,0 sc4 of0.11-0.3）+ 2 槽打 60-66（T3 升级：死列族变体、middle-burst sd40-45 of0.3-0.5）；sd 跨度 30→50 过 W01

## 验证模式（本次用）

ad-hoc 断言脚本放 `%TEMP%/hermes-verify-*.py`：verified 范围/缺口段断言 + 骨架 judge 断言（含豁免边界点：目标值 合格、+2pp 豁免合格、+5pp 不合格）+ 对照组合（现有"最优"应不合格）。12 断言全过，跑完即删。临时分析脚本用完删除，不留项目垃圾。
