# 工具调用流程入口

> **AI 使用提示**：需要代码结构、调用关系或图谱查询时可定位本文；实际优先级和命令以根目录 `AGENTS.md` 为准，本文不单独定义规则。

工具调用与代码检索流程的唯一真源是根目录 [`AGENTS.md`](../../AGENTS.md)。

本文只作为历史链接兼容入口，不重复维护 CodeGraph、codebase-memory-mcp 或 RTK 的优先级、命令和反模式。
