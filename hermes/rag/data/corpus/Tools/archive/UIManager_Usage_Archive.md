# UIManager 使用说明归档

> **AI 使用提示**：本文仅供历史追溯；处理当前 UIManager 时不要直接引用，改读上级目录的现行文档。

归档原 `UIManager` 长说明中的 UI 基线、代码范围、窗口链路、动画约定和 ScrollView 建议。

## 归档内容
- `UIBase` / `UIWindow` / `UIManager`
- `BlastUIWindowView` / `BlastUIBaseView`
- `DOTween` 窗口动画约定
- `ScrollView` 相关建议

## 说明
- 默认入口已迁到 [`../UIManager_Usage.md`](../UIManager_Usage.md)。
